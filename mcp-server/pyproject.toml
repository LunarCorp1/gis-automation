[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "mcp-gis-server"
version = "0.1.0"
description = "MCP server for GIS map automation via PyQGIS"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
]
